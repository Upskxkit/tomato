'use strict'

export const NOTIFICATIONS = {
  SHOW: 'notifications::show'
}

export const WIDGET = {
  OPEN_WIDGET: 'widget::openWidget',
  HIDE_WIDGET: 'widget::hideWidget',
  CLOSE_WIDGET: 'widget::closeWidget'
}
